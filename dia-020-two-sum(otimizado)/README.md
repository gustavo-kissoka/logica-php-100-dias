# Dia 020
Continuação do two sum do **dia 017** com brute force, mas esse é sem loop aninhado com hashmap O(n) ⚡

## lógica 
- criar uma variável como memória
- loop foreach
- condição if com isset para "acelerar"
- no final da condição guardar $mapa[$numero] = $indice;
